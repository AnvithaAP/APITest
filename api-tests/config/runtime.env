# Frequently changing runtime values.
# Update this file once and changes will be reflected across all config consumers.
API_ENV=local
API_BASE_URL=
REQUEST_TIMEOUT_SECONDS=20
DEFAULT_RELEASE=R2026.04-S1
KPI_REPORT_PATH=artifacts/kpi_report.json
KPI_DASHBOARD_PATH=artifacts/kpi_dashboard.md
