Performance suites are executed via k6 scripts by concern (latency/capacity/scalability/stability).
Tag metadata for performance workloads should be carried in orchestrator metadata and canonical report records.
